import React from "react";
import ReactDOM from "react-dom/client";
import TextCompletionApp from "./TextCompletionApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TextCompletionApp />
  </React.StrictMode>
);
